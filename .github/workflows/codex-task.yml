name: Codex Product and Release

on:
  issues:
    types: [labeled]
  repository_dispatch:
    types: [agent_codex_product, agent_codex_release]

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  product-plan:
    if: >
      (github.event_name == 'issues' &&
       github.event.label.name == 'agent:codex' &&
       contains(github.event.issue.labels.*.name, 'phase:product-plan') &&
       contains(github.event.issue.labels.*.name, 'status:todo')) ||
      (github.event_name == 'repository_dispatch' &&
       github.event.action == 'agent_codex_product')
    runs-on: ubuntu-latest
    timeout-minutes: 55

    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
          persist-credentials: false

      - name: Resolve product source issue
        id: source
        env:
          GH_TOKEN: ${{ github.token }}
          ISSUE_NUMBER_EVENT: ${{ github.event.issue.number }}
          ISSUE_NUMBER_DISPATCH: ${{ github.event.client_payload.issue_number }}
        run: |
          ISSUE_NUMBER="${ISSUE_NUMBER_EVENT:-$ISSUE_NUMBER_DISPATCH}"
          test -n "$ISSUE_NUMBER"
          gh issue view "$ISSUE_NUMBER"             --repo "${{ github.repository }}"             --json title,body > "$RUNNER_TEMP/source-issue.json"
          echo "number=$ISSUE_NUMBER" >> "$GITHUB_OUTPUT"
          {
            echo "title<<__TITLE__"
            jq -r '.title' "$RUNNER_TEMP/source-issue.json"
            echo "__TITLE__"
          } >> "$GITHUB_OUTPUT"

      - name: Resolve OpenAI API key
        id: openai_key
        env:
          ORCH_ENV_B64: ${{ secrets.ORCH_ENV_B64 }}
          OPENAI_API_KEY_REPO: ${{ secrets.OPENAI_API_KEY }}
        run: |
          python scripts/runtime_secret.py OPENAI_API_KEY             --fallback-env OPENAI_API_KEY_REPO             --output-name openai_api_key

      - name: Verify resolved OpenAI API key
        env:
          OPENAI_API_KEY_RUNTIME: ${{ steps.openai_key.outputs.openai_api_key }}
        run: test -n "$OPENAI_API_KEY_RUNTIME"

      - name: Require GitHub automation token
        env:
          GH_TOKEN: ${{ github.token }}
        run: test -n "$GH_TOKEN"

      - name: Mark product planning running
        env:
          GH_TOKEN: ${{ github.token }}
          ISSUE_NUMBER: ${{ steps.source.outputs.number }}
        run: |
          gh issue edit "$ISSUE_NUMBER" --repo "${{ github.repository }}"             --remove-label "status:todo"             --add-label "status:running"

      - name: Build product prompt
        env:
          ISSUE_NUMBER: ${{ steps.source.outputs.number }}
          ISSUE_TITLE: ${{ steps.source.outputs.title }}
        run: |
          {
            cat agents/codex_prompt.md
            echo
            echo "## Current task"
            echo "Task ID: GH-ISSUE-$ISSUE_NUMBER"
            echo "Issue: $ISSUE_TITLE"
            echo
            jq -r '.body // ""' "$RUNNER_TEMP/source-issue.json"
          } > "$RUNNER_TEMP/codex-product.md"

      - name: Create task branch
        id: branch
        env:
          ISSUE_NUMBER: ${{ steps.source.outputs.number }}
        run: |
          BRANCH="agent/codex/issue-$ISSUE_NUMBER-${{ github.run_id }}"
          git switch -c "$BRANCH"
          echo "name=$BRANCH" >> "$GITHUB_OUTPUT"

      - name: Run Codex product manager
        id: codex
        continue-on-error: true
        uses: openai/codex-action@v1
        with:
          openai-api-key: ${{ steps.openai_key.outputs.openai_api_key }}
          model: gpt-6-luna
          effort: high
          permission-profile: ":workspace"
          prompt-file: ${{ runner.temp }}/codex-product.md
          output-file: ${{ runner.temp }}/codex-product-output.md

      - name: Validate product-only changes
        if: steps.codex.outcome == 'success'
        id: changed
        run: |
          {
            git diff --name-only
            git ls-files --others --exclude-standard
          } | sed '/^$/d' | sort -u > "$RUNNER_TEMP/changed.txt"

          test -s "$RUNNER_TEMP/changed.txt"

          ALLOWED='^(docs/PRD\.md|docs/RECRUITMENT_RULES\.md|docs/DATA_COLLECTION_PLAN\.md|docs/CLASSIFICATION_DICTIONARY\.md|TASKS\.md|CHANGELOG\.md)$'
          if grep -Ev "$ALLOWED" "$RUNNER_TEMP/changed.txt"; then
            echo "Codex changed files outside the product-planning allowlist."
            exit 1
          fi

          echo "value=true" >> "$GITHUB_OUTPUT"

      - name: Commit and push product plan
        if: steps.codex.outcome == 'success' && steps.changed.outputs.value == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          BRANCH: ${{ steps.branch.outputs.name }}
        run: |
          gh auth setup-git
          git config user.name "codex-product-agent"
          git config user.email "codex-product-agent@users.noreply.github.com"
          git add -A
          git commit -m "docs: Codex product plan"
          git push origin "HEAD:$BRANCH"

      - name: Create specification PR, handoff and dispatch WorkBuddy
        if: steps.codex.outcome == 'success' && steps.changed.outputs.value == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          BRANCH: ${{ steps.branch.outputs.name }}
          ISSUE_NUMBER: ${{ steps.source.outputs.number }}
          ISSUE_TITLE: ${{ steps.source.outputs.title }}
        run: |
          TASK_ID="GH-ISSUE-$ISSUE_NUMBER"

          cat > "$RUNNER_TEMP/pr-body.md" <<EOF
          <!-- agent-task-id:$TASK_ID -->

          Codex product-planning output for issue #$ISSUE_NUMBER.

          Next owner: WorkBuddy prototype/data/classification validation.

          Closes #$ISSUE_NUMBER
          EOF

          PR_URL="$(gh pr create             --repo "${{ github.repository }}"             --base main             --head "$BRANCH"             --title "spec: $ISSUE_TITLE"             --body-file "$RUNNER_TEMP/pr-body.md")"
          PR_NUMBER="${PR_URL##*/}"
          SHA="$(git rev-parse HEAD)"

          ARTIFACTS="$(python - <<'PY'
          import json, os
          from pathlib import Path
          p = Path(os.environ["RUNNER_TEMP"]) / "changed.txt"
          print(json.dumps([line.strip() for line in p.read_text().splitlines() if line.strip()]))
          PY
          )"

          export TASK_ID PR_NUMBER BRANCH SHA ARTIFACTS
          python - <<'PY'
          import json, os
          from pathlib import Path
          payload={
              "version":"1.0",
              "task_id":os.environ["TASK_ID"],
              "from_agent":"codex",
              "to_agent":"workbuddy",
              "phase":"product_planning",
              "status":"success",
              "summary":"Task-specific product-planning specification completed from the source issue.",
              "model":"gpt-6-luna",
              "effort":"high",
              "required_inputs":["source issue","public repository"],
              "expected_outputs":[
                  "reports/prototype_review.md",
                  "reports/data_analysis.md",
                  "reports/classification_validation.md",
                  "reports/uiux_prototype.md"
              ],
              "acceptance":["WorkBuddy must validate implementability before ChatGPT development"],
              "artifacts":json.loads(os.environ["ARTIFACTS"]),
              "checks":[],
              "blockers":[],
              "source_ref":os.environ["BRANCH"],
              "source_sha":os.environ["SHA"],
              "pr_number":int(os.environ["PR_NUMBER"]),
          }
          body="<!-- agent-handoff:v1 -->\n### Agent handoff: codex → workbuddy\n\n```json\n"+json.dumps(payload,ensure_ascii=False,indent=2)+"\n```\n"
          Path(os.environ["RUNNER_TEMP"],"handoff.md").write_text(body)
          PY

          retry() {
            local attempts=0
            local max=5
            local delay=3
            until "$@"; do
              attempts=$((attempts + 1))
              if [ "$attempts" -ge "$max" ]; then
                return 1
              fi
              sleep "$delay"
              delay=$((delay * 2))
            done
          }

          retry gh pr comment "$PR_NUMBER" --repo "${{ github.repository }}"             --body-file "$RUNNER_TEMP/handoff.md"

          retry gh pr edit "$PR_NUMBER" --repo "${{ github.repository }}"             --add-label "phase:prototype"             --add-label "status:todo"             --add-label "agent:workbuddy"

          gh issue edit "$ISSUE_NUMBER" --repo "${{ github.repository }}"             --remove-label "agent:codex"             --remove-label "phase:product-plan"             --remove-label "status:running"             --add-label "status:review" || true

          jq -n             --argjson pr_number "$PR_NUMBER"             --arg task_id "$TASK_ID"             --arg head_ref "$BRANCH"             --arg source_sha "$SHA"             '{
              event_type:"agent_workbuddy_prototype",
              client_payload:{
                pr_number:$pr_number,
                task_id:$task_id,
                head_ref:$head_ref,
                source_sha:$source_sha,
                phase:"prototype"
              }
            }' > "$RUNNER_TEMP/workbuddy-dispatch.json"

          for attempt in 1 2 3 4 5; do
            if gh api --method POST "repos/${{ github.repository }}/dispatches"               --input "$RUNNER_TEMP/workbuddy-dispatch.json"
            then
              exit 0
            fi
            sleep $((attempt * 3))
          done

          echo "Failed to dispatch WorkBuddy after 5 attempts."
          exit 1

      - name: Block failed product planning
        if: steps.codex.outcome != 'success' || steps.changed.outputs.value != 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          ISSUE_NUMBER: ${{ steps.source.outputs.number }}
        run: |
          gh issue edit "$ISSUE_NUMBER" --repo "${{ github.repository }}"             --remove-label "status:running"             --add-label "status:blocked"             --add-label "recovery:technical" || true
          gh issue comment "$ISSUE_NUMBER" --repo "${{ github.repository }}"             --body "<!-- agent-technical-failure:v1:codex:phase:product-plan:issue-$ISSUE_NUMBER --> Codex product-planning execution failed or produced invalid workflow output. This technical phase may be retried by the bounded watchdog; no later phase was dispatched."
          exit 1

  release-review:
    if: github.event_name == 'repository_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 55

    steps:
      - name: Require automation token and resolve PR context
        id: context
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.client_payload.pr_number }}
          TASK_ID_HINT: ${{ github.event.client_payload.task_id }}
          SOURCE_SHA_HINT: ${{ github.event.client_payload.source_sha }}
        run: |
          test -n "$GH_TOKEN"
          test -n "$PR_NUMBER"

          gh api "repos/${{ github.repository }}/pulls/$PR_NUMBER" > "$RUNNER_TEMP/pr.json"

          HEAD_REF="$(jq -r '.head.ref' "$RUNNER_TEMP/pr.json")"
          SOURCE_SHA="$(jq -r '.head.sha' "$RUNNER_TEMP/pr.json")"
          BODY="$(jq -r '.body // ""' "$RUNNER_TEMP/pr.json")"
          LABELS="$(jq -r '.labels[].name' "$RUNNER_TEMP/pr.json")"
          TASK_ID="$(printf '%s' "$BODY" | sed -n 's/.*<!-- agent-task-id:\([A-Za-z0-9._:-]*\) -->.*/\1/p' | head -n1)"

          test -n "$TASK_ID"
          echo "$LABELS" | grep -qx "agent:codex"
          echo "$LABELS" | grep -qx "phase:release"
          echo "$LABELS" | grep -qx "status:todo"

          if [ -n "$TASK_ID_HINT" ] && [ "$TASK_ID_HINT" != "$TASK_ID" ]; then
            exit 1
          fi
          if [ -n "$SOURCE_SHA_HINT" ] && [ "$SOURCE_SHA_HINT" != "$SOURCE_SHA" ]; then
            exit 1
          fi

          echo "pr_number=$PR_NUMBER" >> "$GITHUB_OUTPUT"
          echo "head_ref=$HEAD_REF" >> "$GITHUB_OUTPUT"
          echo "source_sha=$SOURCE_SHA" >> "$GITHUB_OUTPUT"
          echo "task_id=$TASK_ID" >> "$GITHUB_OUTPUT"

      - uses: actions/checkout@v4
        with:
          ref: ${{ steps.context.outputs.head_ref }}
          fetch-depth: 0
          persist-credentials: false

      - name: Resolve OpenAI API key
        id: openai_key
        env:
          ORCH_ENV_B64: ${{ secrets.ORCH_ENV_B64 }}
          OPENAI_API_KEY_REPO: ${{ secrets.OPENAI_API_KEY }}
        run: |
          python scripts/runtime_secret.py OPENAI_API_KEY             --fallback-env OPENAI_API_KEY_REPO             --output-name openai_api_key

      - name: Verify resolved OpenAI API key
        env:
          OPENAI_API_KEY_RUNTIME: ${{ steps.openai_key.outputs.openai_api_key }}
        run: test -n "$OPENAI_API_KEY_RUNTIME"

      - name: Collect and validate WorkBuddy QA handoff
        id: handoff_gate
        continue-on-error: true
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ steps.context.outputs.pr_number }}
          SOURCE_SHA: ${{ steps.context.outputs.source_sha }}
          TASK_ID: ${{ steps.context.outputs.task_id }}
        run: |
          gh api "repos/${{ github.repository }}/issues/$PR_NUMBER/comments?per_page=100"             --paginate > "$RUNNER_TEMP/comments.json"

          PYTHONPATH=. python scripts/validate_handoff.py             --comments "$RUNNER_TEMP/comments.json"             --task-id "$TASK_ID"             --from-agent workbuddy             --to-agent codex             --phase qa_acceptance             --source-sha "$SOURCE_SHA"             --trusted-login "${{ github.repository_owner }}"             --trusted-login "github-actions[bot]"

          jq -r '.[] | select(.body | contains("<!-- agent-handoff:v1 -->")) | .body'             "$RUNNER_TEMP/comments.json" > "$RUNNER_TEMP/handoffs.md"

      - name: Block invalid release handoff gate
        if: steps.handoff_gate.outcome != 'success'
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ steps.context.outputs.pr_number }}
        run: |
          gh pr edit "$PR_NUMBER" --repo "${{ github.repository }}"             --remove-label "status:todo"             --add-label "status:blocked" || true
          gh pr comment "$PR_NUMBER" --repo "${{ github.repository }}"             --body "Codex release review was not started because the required successful WorkBuddy QA handoff was invalid."
          exit 1

      - name: Require release-enabled terminal policy or exact owner release approval
        id: terminal_gate
        continue-on-error: true
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ steps.context.outputs.pr_number }}
          TASK_ID: ${{ steps.context.outputs.task_id }}
          SOURCE_SHA: ${{ steps.context.outputs.source_sha }}
          OWNER: ${{ github.repository_owner }}
        run: |
          set -euo pipefail
          ISSUE_NUMBER="$(printf '%s' "$TASK_ID" | sed -n 's/^GH-ISSUE-\([0-9][0-9]*\)$/\1/p')"
          BODY=""
          if [ -n "$ISSUE_NUMBER" ]; then
            BODY="$(gh issue view "$ISSUE_NUMBER" --repo "${{ github.repository }}" --json body --jq '.body // ""')"
          fi
          export BODY
          RELEASE_ENABLED="$(PYTHONPATH=. python - <<'PY'
          import os
          from orchestrator.terminal_policy import resolve_terminal_policy
          print("true" if resolve_terminal_policy(os.environ.get("BODY", "")).release_enabled else "false")
          PY
          )"
          if [ "$RELEASE_ENABLED" = "true" ]; then
            exit 0
          fi

          LABELS="$(gh pr view "$PR_NUMBER" --repo "${{ github.repository }}" --json labels --jq '.labels[].name')"
          echo "$LABELS" | grep -qx "approval:production-approved"

          gh api "repos/${{ github.repository }}/issues/$PR_NUMBER/comments?per_page=100" --paginate > "$RUNNER_TEMP/release-approval-comments.json"
          python - <<'PY'
          import json, os
          from pathlib import Path
          comments=json.loads(Path(os.environ["RUNNER_TEMP"],"release-approval-comments.json").read_text())
          owner=os.environ["OWNER"]; task=os.environ["TASK_ID"]; sha=os.environ["SOURCE_SHA"]
          for comment in reversed(comments):
              if (comment.get("user") or {}).get("login") != owner:
                  continue
              body=str(comment.get("body") or "")
              if "<!-- release-approval:v1 -->" not in body:
                  continue
              if all(x in body for x in (f"task_id={task}", f"source_sha={sha}", "approved=true")):
                  raise SystemExit(0)
          raise SystemExit("release requires release_enabled policy or exact-SHA owner approval")
          PY

      - name: Hold release in owner review
        if: steps.terminal_gate.outcome != 'success'
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ steps.context.outputs.pr_number }}
        run: |
          gh pr edit "$PR_NUMBER" --repo "${{ github.repository }}"             --remove-label "agent:codex"             --remove-label "phase:release"             --remove-label "status:todo"             --remove-label "status:running"             --add-label "status:review"             --add-label "approval:production-required"
          gh pr comment "$PR_NUMBER" --repo "${{ github.repository }}"             --body "⏸️ Release stopped fail-closed: terminal policy does not permit automatic release and no exact-SHA owner release approval was found."

      - name: Mark release review running
        if: steps.terminal_gate.outcome == 'success'
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ steps.context.outputs.pr_number }}
        run: |
          gh pr edit "$PR_NUMBER" --repo "${{ github.repository }}"             --remove-label "status:todo"             --add-label "status:running"

      - name: Build release prompt
        if: steps.terminal_gate.outcome == 'success'
        env:
          PR_NUMBER: ${{ steps.context.outputs.pr_number }}
          TASK_ID: ${{ steps.context.outputs.task_id }}
        run: |
          {
            cat agents/codex_prompt.md
            echo
            echo "## Release-review target"
            echo "Task ID: $TASK_ID"
            echo "PR #$PR_NUMBER"
            echo
            echo "## Prior handoffs"
            cat "$RUNNER_TEMP/handoffs.md"
          } > "$RUNNER_TEMP/codex-release.md"

      - name: Run Codex release review
        if: steps.terminal_gate.outcome == 'success'
        id: codex
        continue-on-error: true
        uses: openai/codex-action@v1
        with:
          openai-api-key: ${{ steps.openai_key.outputs.openai_api_key }}
          model: gpt-6-luna
          effort: high
          permission-profile: ":workspace"
          prompt-file: ${{ runner.temp }}/codex-release.md
          output-file: ${{ runner.temp }}/codex-release-output.md

      - name: Validate release-only changes
        if: steps.codex.outcome == 'success'
        id: release_files
        run: |
          {
            git diff --name-only
            git ls-files --others --exclude-standard
          } | sed '/^$/d' | sort -u > "$RUNNER_TEMP/changed.txt"

          ALLOWED='^(CHANGELOG\.md|docs/RELEASE_NOTES\.md|reports/release_gate\.json)$'
          if grep -Ev "$ALLOWED" "$RUNNER_TEMP/changed.txt"; then
            echo "Codex changed files outside the release-review allowlist."
            exit 1
          fi

          test -f reports/release_gate.json
          python -m json.tool reports/release_gate.json >/dev/null
          echo "value=true" >> "$GITHUB_OUTPUT"

      - name: Commit release documents
        if: steps.codex.outcome == 'success' && steps.release_files.outputs.value == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          HEAD_REF: ${{ steps.context.outputs.head_ref }}
        run: |
          gh auth setup-git
          git config user.name "codex-release-agent"
          git config user.email "codex-release-agent@users.noreply.github.com"
          git add CHANGELOG.md docs/RELEASE_NOTES.md reports/release_gate.json
          git commit -m "docs: Codex release review" || true
          git push origin "HEAD:$HEAD_REF"

      - name: Apply release gate and dispatch WorkBuddy deployment
        if: steps.codex.outcome == 'success' && steps.release_files.outputs.value == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ steps.context.outputs.pr_number }}
          HEAD_REF: ${{ steps.context.outputs.head_ref }}
          TASK_ID: ${{ steps.context.outputs.task_id }}
        run: |
          STATUS="$(python - <<'PY'
          import json
          print(json.load(open("reports/release_gate.json"))["status"])
          PY
          )"
          SUMMARY="$(python - <<'PY'
          import json
          print(json.load(open("reports/release_gate.json")).get("summary",""))
          PY
          )"
          SHA="$(git rev-parse HEAD)"

          if [ "$STATUS" != "ready" ]; then
            gh pr edit "$PR_NUMBER" --repo "${{ github.repository }}"               --remove-label "status:running"               --add-label "status:blocked" || true
            gh pr comment "$PR_NUMBER" --repo "${{ github.repository }}"               --body "Codex release gate blocked: $SUMMARY"
            exit 1
          fi

          export STATUS SUMMARY SHA
          python - <<'PY'
          import json, os
          from pathlib import Path
          payload={
              "version":"1.0",
              "task_id":os.environ["TASK_ID"],
              "from_agent":"codex",
              "to_agent":"workbuddy",
              "phase":"release_review",
              "status":"success",
              "summary":os.environ.get("SUMMARY",""),
              "model":"gpt-6-luna",
              "effort":"high",
              "required_inputs":["WorkBuddy QA reports","ChatGPT implementation","product specification"],
              "expected_outputs":["WorkBuddy deployment plan","machine-readable deployment gate"],
              "acceptance":["WorkBuddy deployment gate must be ready before automatic merge/deployment"],
              "artifacts":["CHANGELOG.md","docs/RELEASE_NOTES.md","reports/release_gate.json"],
              "checks":[],
              "blockers":[],
              "source_ref":os.environ["HEAD_REF"],
              "source_sha":os.environ["SHA"],
              "pr_number":int(os.environ["PR_NUMBER"]),
          }
          body="<!-- agent-handoff:v1 -->\n### Agent handoff: codex → workbuddy\n\n```json\n"+json.dumps(payload,ensure_ascii=False,indent=2)+"\n```\n"
          Path(os.environ["RUNNER_TEMP"],"handoff.md").write_text(body)
          PY

          gh pr comment "$PR_NUMBER" --repo "${{ github.repository }}"             --body-file "$RUNNER_TEMP/handoff.md"

          gh pr edit "$PR_NUMBER" --repo "${{ github.repository }}"             --remove-label "agent:codex"             --remove-label "phase:release"             --remove-label "status:running"             --remove-label "approval:production-required"             --add-label "agent:workbuddy"             --add-label "phase:deploy"             --add-label "status:todo"

          jq -n             --argjson pr_number "$PR_NUMBER"             --arg task_id "$TASK_ID"             --arg head_ref "$HEAD_REF"             --arg source_sha "$SHA"             '{
              event_type:"agent_workbuddy_deploy",
              client_payload:{
                pr_number:$pr_number,
                task_id:$task_id,
                head_ref:$head_ref,
                source_sha:$source_sha,
                phase:"deploy"
              }
            }' | gh api --method POST "repos/${{ github.repository }}/dispatches" --input -

      - name: Block failed release review
        if: steps.terminal_gate.outcome == 'success' && (steps.codex.outcome != 'success' || steps.release_files.outputs.value != 'true')
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ steps.context.outputs.pr_number }}
          SOURCE_SHA: ${{ steps.context.outputs.source_sha }}
        run: |
          gh pr edit "$PR_NUMBER" --repo "${{ github.repository }}"             --remove-label "status:running"             --add-label "status:blocked"             --add-label "recovery:technical" || true
          gh pr comment "$PR_NUMBER" --repo "${{ github.repository }}"             --body "<!-- agent-technical-failure:v1:codex:phase:release:$SOURCE_SHA --> Codex release-review execution failed or produced invalid workflow output. This technical phase may be retried by the bounded watchdog at the same exact SHA."
          exit 1
